---
const nav = [
  { href: '/', label: 'Home' },
  { href: '/about', label: 'Studio' },
  { href: '/projects', label: 'Projects' },
  { href: '/contact', label: 'Contact' },
];
---

<header class="border-b border-surface/70 bg-iron/95 backdrop-blur-md sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-5 sm:px-8 lg:px-12 h-16 sm:h-[4.75rem] flex items-center justify-between">
    <a href="/" class="flex items-center group" aria-label="SlushTec home">
      <img
        src="/brand/lockup-primary.jpg"
        alt="SlushTec"
        class="h-9 sm:h-11 w-auto object-contain transition duration-200 group-hover:opacity-90"
        height="44"
      />
    </a>
    <nav class="flex items-center gap-6 sm:gap-8 text-sm tracking-wide">
      {nav.map((item) => (
        <a
          href={item.href}
          class="text-gray-400 hover:text-warm-earth transition"
        >
          {item.label}
        </a>
      ))}
    </nav>
  </div>
</header>
